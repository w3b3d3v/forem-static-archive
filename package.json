{
  "name": "forem-static-archive",
  "version": "1.0.0",
  "description": "Static archive of Forem articles with SEO optimization and mobile-responsive design",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "npx http-server public -p 8080 -o",
    "clean": "rm -rf public/*"
  },
  "dependencies": {
    "csv-parser": "^3.0.0",
    "marked": "^11.0.0",
    "sanitize-html": "^2.11.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
